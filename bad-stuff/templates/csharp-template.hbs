using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace {{namespace}}
{
    /// <summary>
    /// TODO: write {{className}} summary
    /// </summary>
    public class {{className}} {{#if base}}: {{base}}{{/if}}
    {
        /// <summary>
        /// TODO: write {{className}} constructor summary
        /// </summary>
        public {{className}}() { }

    {{#each fields}}

        /// <summary>
        /// TODO: write {{../className}}.{{fieldName}} summary
        /// </summary>
        {{access}}{{#if isNullable}}Nullable<{{/if}}{{type}}{{#if isNullable}}>{{/if}} {{fieldName}} { get; set; }
    {{/each}}
    }
}
