{{#each dependencies}}
import {{namespace.pascalCase}} from {{namespace.namespacePascalCase}};
{{/each}}

/** {{className.pascalCase}}
 ** TODO: write script doc
 **/
export class {{className.pascalCase}} {{#if supertype}}extends {{supertype.className.pascalCase}}{{/if}}
{
    constructor() { }
{{#each fields}}

    set {{fieldName.camelCase}}({{fieldName.camelCase}}) {
      this._{{fieldName.camelCase}} = {{fieldName.camelCase}};
    }

    get {{fieldName.camelCase}}() {
      return this._{{fieldName.camelCase}};
    }
{{/each}}
}
