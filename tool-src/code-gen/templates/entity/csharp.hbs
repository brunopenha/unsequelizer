{{#each dependencies}}
using {{namespace.namespacePascalCase}};
{{/each}}
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace {{namespace.namespacePascalCase}}
{
    /// <summary>
    /// TODO: write {{className.pascalCase}} summary
    /// </summary>
    public class {{className.pascalCase}} {{#if base}}: {{base.pascalCase}}{{/if}}
    {
        /// <summary>
        /// TODO: write {{className.pascalCase}} constructor summary
        /// </summary>
        public {{className.pascalCase}}() { }
    {{#each fields}}

        /// <summary>
        /// TODO: write {{../className.pascalCase}}.{{fieldName.pascalCase}}{{#if isCollection}}s{{/if}} summary
        /// </summary>
        public {{#unless isClassReference}}{{#if isNullable}}Nullable<{{/if}}{{/unless}}{{#if isCollection}}List<{{/if}}{{#if isClassReference}}{{type.pascalCase}}{{else}}{{{type}}}{{/if}}{{#if isCollection}}>{{/if}}{{#unless isClassReference}}{{#if isNullable}}>{{/if}}{{/unless}} {{fieldName.pascalCase}}{{#if isCollection}}s{{/if}} { get; set; }
    {{/each}}
    }
}
